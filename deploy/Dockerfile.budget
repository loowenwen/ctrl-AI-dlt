FROM public.ecr.aws/lambda/python:3.12

WORKDIR /var/task

# Copy repo contents
COPY . .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Lambda handler (module.function)
CMD ["agents.bto_budget_estimator_lambda.handler"]

